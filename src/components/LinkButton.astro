---
const { href, target = '_blank', rel = 'noopener noreferrer', bgColor, borderColor, ...rest } = Astro.props;
---
<a
  href={href}
  target={target}
  rel={rel}
  {...rest}
  class={`text-md inline-flex items-center justify-center gap-2 px-4 py-1 md:py-2
    text-sky-950 bg-transparent border rounded-full shadow-md
    hover:shadow-xl hover:scale-105 transition-transform duration-300 cursor-pointer mb-2
    ${bgColor} ${borderColor}`}
>
  <slot />
</a>